name: my-project
context:
    Name: My Project
beforeCopy:
    steps:
        - name: install-deps
          command: echo
          args:
            - ${projectName}/package.json
afterCopy:
    steps:
        # - name: build
        #   command: npm
        #   args:
        #     - run
        #     - build
